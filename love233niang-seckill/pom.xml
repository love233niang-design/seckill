<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.8</version>
    <relativePath/> <!-- lookup parent from repository -->
  </parent>

  <groupId>com.love233niang</groupId>
  <artifactId>love233niang-seckill</artifactId>
  <version>${revision}</version>
  <!-- 项目名称 -->
  <name>${project.artifactId}</name>
  <!-- 项目描述 -->
  <description>秒杀系统 By hq</description>

  <packaging>pom</packaging>

  <!-- 子模块管理 -->
  <modules>
    <!-- 公共模块 -->
    <module>love233niang-seckill-common</module>
    <!-- 用户模块 -->
    <module>love233niang-seckill-user</module>
    <!-- 商品模块 -->
    <module>love233niang-seckill-goods</module>
    <!-- 订单模块 -->
    <module>love233niang-seckill-order</module>
    <!-- 启动入口 -->
    <module>love233niang-seckill-app</module>
  </modules>


  <!-- 版本号统一管理 -->
  <properties>
    <!-- 项目版本号 -->
    <revision>0.0.1-SNAPSHOT</revision>
    <!-- JDK 版本 -->
    <java.version>21</java.version>
    <maven.compiler.source>${java.version}</maven.compiler.source>
    <maven.compiler.target>${java.version}</maven.compiler.target>
    <!-- 项目编码 -->
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- MyBatis 版本 -->
    <mybatis-spring-boot.version>3.0.5</mybatis-spring-boot.version>
    <mybatis-generator-maven-plugin.version>1.3.5</mybatis-generator-maven-plugin.version>
    <disruptor.version>3.4.4</disruptor.version>
    <!-- SaToken 版本 -->
    <sa-token.version>1.45.0</sa-token.version>
    <!-- Hutool 版本 -->
    <hutool.version>5.8.38</hutool.version>
    <!-- Redisson 版本 -->
    <redisson.version>3.52.0</redisson.version>
  </properties>

  <!-- 统一依赖管理 -->
  <dependencyManagement>
    <dependencies>

      <!-- 公共模块 -->
      <dependency>
        <groupId>com.love233niang</groupId>
        <artifactId>love233niang-seckill-common</artifactId>
        <version>${revision}</version>
      </dependency>

      <!-- 用户模块 -->
      <dependency>
        <groupId>com.love233niang</groupId>
        <artifactId>love233niang-seckill-user</artifactId>
        <version>${revision}</version>
      </dependency>

      <!-- 商品模块 -->
      <dependency>
        <groupId>com.love233niang</groupId>
        <artifactId>love233niang-seckill-goods</artifactId>
        <version>${revision}</version>
      </dependency>

      <dependency>
        <groupId>com.love233niang</groupId>
        <artifactId>love233niang-seckill-order</artifactId>
        <version>${revision}</version>
      </dependency>

      <!-- MyBatis Spring Boot Starter -->
      <dependency>
        <groupId>org.mybatis.spring.boot</groupId>
        <artifactId>mybatis-spring-boot-starter</artifactId>
        <version>${mybatis-spring-boot.version}</version>
      </dependency>

      <!-- Disruptor 高性能队列（Log4j2 异步日志需要） -->
      <dependency>
        <groupId>com.lmax</groupId>
        <artifactId>disruptor</artifactId>
        <version>${disruptor.version}</version>
      </dependency>

      <!-- SaToken 权限认证框架（适配 Spring Boot 3） -->
      <dependency>
        <groupId>cn.dev33</groupId>
        <artifactId>sa-token-spring-boot3-starter</artifactId>
        <version>${sa-token.version}</version>
      </dependency>

      <!-- Hutool 工具类库 -->
      <dependency>
        <groupId>cn.hutool</groupId>
        <artifactId>hutool-all</artifactId>
        <version>${hutool.version}</version>
      </dependency>

      <!-- Sa-Token 整合 RedisTemplate -->
      <dependency>
        <groupId>cn.dev33</groupId>
        <artifactId>sa-token-redis-template</artifactId>
        <version>${sa-token.version}</version>
      </dependency>

      <!-- Redisson（提供布隆过滤器等高级数据结构） -->
      <dependency>
        <groupId>org.redisson</groupId>
        <artifactId>redisson-spring-boot-starter</artifactId>
        <version>${redisson.version}</version>
      </dependency>

    </dependencies>
  </dependencyManagement>

  <build>
    <!-- 统一插件管理 -->
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <executions>
            <execution>
              <id>repackage</id>
              <goals>
                <goal>repackage</goal> <!-- 将依赖的 Jar 一起打包 -->
              </goals>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <configuration>
            <source>${java.version}</source>
            <target>${java.version}</target>
            <encoding>${project.build.sourceEncoding}</encoding>
            <annotationProcessorPaths>
              <path>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
              </path>
            </annotationProcessorPaths>
          </configuration>
        </plugin>

        <!-- 代码生成器 -->
        <plugin>
          <groupId>org.mybatis.generator</groupId>
          <artifactId>mybatis-generator-maven-plugin</artifactId>
          <version>${mybatis-generator-maven-plugin.version}</version>
          <configuration>
            <!-- 允许移动生成的文件 -->
            <verbose>true</verbose>
            <!-- 允许覆盖生成的文件 -->
            <overwrite>true</overwrite>
          </configuration>
          <!-- 此插件需要连接数据库，所以需要依赖 MySQL 驱动 -->
          <dependencies>
            <dependency>
              <groupId>com.mysql</groupId>
              <artifactId>mysql-connector-j</artifactId>
              <version>${mysql.version}</version>
            </dependency>
          </dependencies>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

</project>
